/**
 * Task 1: Translate a Simple Program (Python → JavaScript)
 * This module contains a JavaScript function that prints the first 10 natural numbers.
 */

function printNumbers() {
    /**
     * Print the first 10 natural numbers using a loop.
     */
    for (let i = 1; i <= 10; i++) {
        console.log(i);
    }
}

// Call the function to display results
printNumbers();
